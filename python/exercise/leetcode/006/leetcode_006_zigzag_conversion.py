test_data = "PAYPALISHIRING"

def zigzag_converter(data, rows):
    result = ""
    current_row = 0;
    front_index = 0
    end_index = 0
    for x in range(0, len(data)):
        if front_index >= len(data):
            break

        for y in range(x, )
